name: cuma ngetes

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout repo
        uses: actions/checkout@v4

      - name: ngetes workflow
        run: |
          echo "action berhasil jalan tanpa error, selesai"
